# SR 281 - home24 Germany (Germany)
site URL: https://www.home24.de/
brand site: home24.de

## Prior Home-Decor pass (archive c159) status=failed
FAILURE: POLICY BLOCK (duplicate finding to S.N. 113 'Home24 DE', same domain home24.de). Fresh robots.txt re-check on 2026-08-11 confirms the policy is unchanged: a direct/unimpersonated fetch of https://www.home24.de/robots.txt returns HTTP 403 (site WAF block page), but a curl_cffi chrome131-impersonated fetch returns HTTP 200 with the same robots.txt content S.N. 113 recorded: 'User-agent: * / Disallow: /checkout/, /meinkundenkonto/, /myaccount/, /pdp-app/, /search, /article/recordvisit, /contenttree/plp/' followed by 'User-agent: Pinterestbot / Crawl-delay: 2', 'User-agent: Amazonbot / Disallow: /', and 'User-agent: ClaudeBot / Disallow: /' (byte-for-byte identical directive set to c113.json). The ClaudeBot block is a targeted, Claude-specific full-site disallow, separate from and more restrictive than the generic '*' group (which only blocks a handful of account/checkout paths for everyone). Per the S.N.113 precedent and this batch's brief, this is treated as a deliberate opt-out of Claude/Anthropic access, not a generic anti-bot wall to route around. No WAF-bypass or extraction was attempted beyond this confirmation fetch.
ACCESS/METHOD NOTES: Duplicate-company / known-issue flag from BRIEF.md confirmed: S.N. 159 is the same domain (home24.de) as already-completed S.N. 113 'Home24 DE' (failed, same reasoning). Read c113.json first per instructions. Did the required fresh robots.txt check (policies occasionally change) -- result is unchanged from c113: ClaudeBot is still explicitly disallowed site-wide. No bypass attempted, consistent with the project's stance of respecting a targeted Claude-specific opt-out rather than routing around it. No category tree, qty, or product data was extracted (only robots.txt was fetched, once directly (403 WAF) and once via curl_cffi impersonation (200) purely to read the policy text). This is now the second S.N. in this project (113, 159) to hit the identical home24.de ClaudeBot block -- consistent, not a one-off.

Verified working category URLs from that pass (0) - these prove the site is reachable and show its URL shape:
