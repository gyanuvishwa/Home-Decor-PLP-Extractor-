# SR 285 - Amazon UAE (UAE)
site URL: amazon.ae
brand site: amazon.ae

## Prior Home-Decor pass (archive c189) status=failed
FAILURE: POLICY BLOCK, not a technical block: amazon.ae's robots.txt (https://www.amazon.ae/robots.txt, direct fetch HTTP 200) contains FOUR separate directives that specifically and by name disallow Anthropic/Claude crawlers with a full-site 'Disallow: /': 'User-agent: ClaudeBot' (listed twice, lines 111 and 198), 'User-agent: Claude-User' (line 195), 'User-agent: Claude-SearchBot' (line 201), and 'User-agent: Claude-Web' (line 258) - all four 'Disallow: /'. This is distinct from and far more restrictive than the generic 'User-agent: *' group (lines 1-88), which only disallows a specific list of account/cart/review/wishlist/signin utility paths and otherwise permits crawling. This matches the exact pattern this project has already confirmed as a deliberate, targeted Claude/Anthropic opt-out on S.N. 113 'Home24 DE', S.N. 159 'home24 Germany' (same domain, re-confirmed), and El Corte Ingles - per the brief's explicit instruction ('Check robots.txt for a Claude-specific disallow before any WAF-bypass') and this project's established precedent of respecting such directives rather than routing around them, I stopped before any category/qty extraction. Separately, an initial direct homepage fetch (made before the robots.txt content was read, using a generic desktop-Chrome UA, not identifying as Claude) returned HTTP 202 with an AWS WAF JS-challenge interstitial page (awswaf.com challenge.js) - this confirms the site is live and actively serving (not retired/shut down), so this is purely a policy stop, not a claim of technical unreachability. No WAF-bypass technique (curl_cffi impersonation, r.jina.ai, translate.goog, etc.) was attempted once the robots.txt finding surfaced. No category tree, qty, or product data was extracted beyond the homepage and robots.txt access-route checks made in the course of the Rule 3 sanity check.
ACCESS/METHOD NOTES: STOPPED BEFORE EXTRACTION on discovering amazon.ae/robots.txt names ClaudeBot (x2), Claude-User, Claude-SearchBot, and Claude-Web, all with 'Disallow: /', separate from and more restrictive than the generic '*' group. Consistent with this project's established stance (S.N. 113, S.N. 159 Home24 DE; El Corte Ingles) of treating a targeted Claude-specific full-site robots.txt disallow as a deliberate opt-out to respect, not a generic anti-bot wall to route around per Rule 2's WAF-bypass ladder (which assumes no such targeted directive exists). This is a deliberate policy decision by this worker, not a claim that the site is technically unreachable - the homepage's AWS WAF JS-challenge response shows the site is live and merely bot-protected, and the brief's own note that Amazon UAE is a huge general marketplace requiring strict Home/Decor department scoping was never reached because extraction did not begin. Flagging for the orchestrator: S.N. 191 'Noon UAE', also flagged in this brief as a marketplace requiring the same careful department scoping, should get the same robots.txt check before any extraction work, as should any other company in this rolling pool not yet checked.

Verified working category URLs from that pass (0) - these prove the site is reachable and show its URL shape:
