# SR 282 - El Corte Inglés Home (Spain)
site URL: https://www.elcorteingles.es/
brand site: elcorteingles.es

## Prior Home-Decor pass (archive c129) status=failed
FAILURE: robots.txt at https://www.elcorteingles.es/robots.txt explicitly disallows Claude: 'User-agent: ClaudeBot / Disallow: /' and 'User-agent: Claude-User / Disallow: /' (site-wide, no path exceptions). Per project rule, this is treated as a real access boundary rather than something to bypass, following the same precedent as Made In Design and Home24 DE in earlier batches.
ACCESS/METHOD NOTES: Direct requests-based fetch of robots.txt returned HTTP 403 from an Akamai edge (errors.edgesuite.net reference), so curl_cffi impersonation (chrome131) was used ONLY to retrieve and read robots.txt itself to check for a Claude-specific disallow, per the brief's mandatory pre-check. That check found explicit ClaudeBot and Claude-User disallow rules covering the entire site. No further pages (category tree, listings, counts) were fetched or bypassed once this was found -- extraction was stopped immediately per instructions. rows left empty accordingly.

Verified working category URLs from that pass (0) - these prove the site is reachable and show its URL shape:
