# SR 284 - Home Box UAE (UAE)
site URL: https://www.homeboxstores.com/ae/en/
brand site: homeboxstores.com

## Prior Home-Decor pass (archive c187) status=failed
FAILURE: Entire domain (apex, www, all subpaths including robots.txt, sitemap XML, and even static /_next/static JS chunks) is behind a Cloudflare 'Managed Challenge' (Turnstile, interactive JS challenge, HTTP 403 'Just a moment...' page) on every route tested. Not a targeted anti-Claude block: wayback-cached robots.txt (snapshot 2025-02-07) shows only a generic User-agent:* group plus explicit disallows for MJ12bot/EtaoSpider/CazoodleBot/dotbot/Gigabot -- no ClaudeBot/anthropic-ai/Claude-Web/Claude-User entry, so this is a blanket bot wall, not a Claude-specific gate.
ACCESS/METHOD NOTES: No duplicate-company flag for S.N. 187 in the brief (only 179/182/194 flagged) -- treated as independent, no-precedent UAE extraction per the brief's explicit callout. Full Rule 2 access sweep, all failed: (1) direct fetch -- 403 Cloudflare challenge on '/', '/robots.txt', apex domain, and a static JS chunk URL (challenge applies uniformly, confirming it's an edge-level WAF rule, not a document-only check). (2) curl_cffi impersonation, 18 browser profiles (chrome99-131 incl. android, edge99/101, safari15.3-17.2ios, firefox133) -- all 403 identical 'Just a moment...' Turnstile page. (3) r.jina.ai reader proxy on the homepage -- 403, same challenge page returned through the proxy. (4) translate.goog (www-homeboxstores-com.translate.goog) -- 403, same Cloudflare challenge page served through Google's proxy IP. (5) WebFetch tool -- 403 Forbidden, no body retrieved. (6) Side doors: sitemap.xml, sitemap_index.xml, and the locale-specific /ae/en/sitemapindex.xml (URL recovered from a wayback-cached robots.txt) all 403'd identically; api.homeboxstores.com and m.homeboxstores.com do not resolve/connect at all. NOTABLE FIND: wayback snapshots of the live site (most recent usable: 2026-05-25 homepage, 2025-03-23 category page) embed a live Algolia search config -- algoliaAppId '3HWOWX4270', algoliaProductIndex 'prod_uae_homebox_Product', algoliaCategoryIndex 'prod_uae_homebox_Category', and a search-only API key. Queried the Algolia REST API directly (https://3HWOWX4270-dsn.algolia.net and 4 alternate host forms) with that key -- every request returns HTTP 403 '{"message":"Account temporary disabled for security reasons, please contact us","status":403}', i.e. the Algolia application itself is disabled account-wide (not a bad/rotated key), so this side door is a dead end regardless of key freshness. (7) Akamai POW solver not applicable -- this is Cloudflare, not Akamai. (8) IPv6 anycast pinning not attempted: the challenge served here is Cloudflare's interactive Managed Challenge (Turnstile widget, cType:'managed' in the page's _cf_chl_opt), which requires actual JS/browser execution to solve, not merely IP-reputation or TLS/JA3 fingerprint evasion -- IP pinning has no mechanism to defeat an interactive challenge, and browser automation is explicitly disallowed for this pool (shared browser). Wayback CDX enumeration did recover ~1800 /ae/en/c/<slug> category-listing URLs (e.g. bathroom-mirrors, bathroom-bathroomaccessories, plus baby/beauty/apparel-heavy slugs suggesting Home Box is a broad hypermarket, not a decor-only retailer) confirming the site has an accessible category tree in principle, but per Rule 2 Wayback is structure-only and never usable for qty, and no canonical live link/qty could be verified for any node, so no rows could be emitted at leaf or parent level without violating the qty-evidence and canonical-link requirements. Recommend a fresh attempt later (Cloudflare challenge state can change) or via a worker with browser-automation clearance.

Verified working category URLs from that pass (0) - these prove the site is reachable and show its URL shape:
